# stack-and-queue

The idea of stack is any data that get into stack last is comes out first(LIFO)
   the stack is implemented using linkedlist 
   
The idea of queue is any data that get into queue first is comes out first(FIFO).
       the queue is implemented using linkedlist and 2 stacks
